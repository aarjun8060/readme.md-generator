interface DownloadModalProps {
    setShowModal: () => void;
}
export const DownloadModal = ({
    setShowModal
}:DownloadModalProps) => {
    return (
        <div>
            DownLoad Modal
        </div>
    )
}